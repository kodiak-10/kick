# Map provider credential. Keep the actual value only in backend/.env.
KICK_AMAP_KEY=replace_with_your_amap_key

# Enables the public OpenStreetMap fallback when a provider lookup is unavailable.
KICK_ENABLE_OSM_FALLBACK=true
